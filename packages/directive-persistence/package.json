{
  "name": "@infiniclaw/directive-persistence",
  "version": "0.0.0",
  "description": "OpenClaw plugin: DIRECTIVES/JOURNAL split, verifier-authority, sub-agent recursion. See BUILD.md at repo root for the full spec.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "templates/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "yaml": "^2.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
