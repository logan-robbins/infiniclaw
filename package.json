{
  "name": "infiniclaw",
  "version": "0.0.0",
  "private": true,
  "description": "Persistent directive system for long-horizon OpenClaw agents",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/logan-robbins/infiniclaw.git"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
